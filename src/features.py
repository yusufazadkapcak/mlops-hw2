def hash_feature(input_string, num_buckets=10):
    # SABOTAJ
    raise ValueError("The system has been hacked! There's a bug!")
    
    
    # if not input_string:
    #     return 0
    # return hash(input_string) % num_buckets